/**
 * test methods that get from db
 */
import { getEvent } from '../src/utils/db/event';

describe('test XXX', function () {
	this.timeout(3 * 1000);
	it('should do something', async () => {
		await getEvent('test');
	});
});
